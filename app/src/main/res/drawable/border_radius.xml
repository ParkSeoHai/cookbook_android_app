<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền của View -->
    <solid android:color="#FFFFFF" />

    <!-- Độ bo góc -->
    <corners android:radius="20dp" />

    <!-- Đường viền xung quanh (tuỳ chọn) -->
    <stroke
        android:width="1dp"
        android:color="#fff" /> <!-- Màu cho border -->
</shape>
